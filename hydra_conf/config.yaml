defaults:
  - env: LayerwiseSGD/MNIST

results_dir: ???  # Mandatory, no default value
num_train_iter: 30000
val_freq: 30000
seed: 0
eval_seed: 0
eval_protocol: "train"
teacher: "step_decay"
instance_mode: null
id: 0
agent_type: "td3_bc"
tanh_scaling: false
combination: "single"
mode: "all"  # Options: "data_gen", "train", "eval", "all"
data_exists: false
wandb_group: "DACORL"
dataset_path: "./datasets"
device: "auto"  # Add device configuration - use "auto" to detect, "cuda" for GPU, "cpu" for CPU